version: v2

modules:
  - path: api/protos

deps:
  - buf.build/googleapis/googleapis:latest

lint:
  use:
    - STANDARD
  #except:
  #  - PACKAGE_VERSION_SUFFIX  # demo.v1 zorlamasını kaldırır
  #  - DIRECTORY_SAME_PACKAGE  # “package demo ise demo/ dizininde olmalı” zorlamasını kaldırır

breaking:
  use:
    - FILE
